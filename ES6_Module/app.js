import React from './react.js' // Có thể đặt tên khác, code tự nhận là giá trị mặc định của export default: import ReactParam from './react.js'
import { fn, value as valueParam } from './react.js' // Sử dụng as để đổi tên

console.log(React)
console.log(valueParam)
fn()
